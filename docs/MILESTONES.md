# Milestones do projeto

Mapa dos pontos importantes da história do projeto. Cada milestone é uma **tag anotada**
do git — permanente, nomeada e à prova de GC/reflog. Para voltar a um milestone:

```bash
git switch -c <nome-do-branch> milestone-<data>-<nome>   # ex.:
git switch -c retorno-milestone milestone-2026-08-07-librashader-funcional
```

Novos milestones: `tools/milestone.sh <nome-curto> "<descrição>"` (cria tag + registra aqui + push).

---

| Milestone (tag) | Commit (HEAD na época) | Data | O que marca |
|---|---|---|---|
| `milestone-2026-08-07-librashader-funcional` | `12de2797` | 2026-08-07 | **Shaders RetroArch (librashader) FUNCIONANDO no device** (Xiaomi Mi 11/Adreno 650) **+ pull do upstream utkarshdalal:master incluído** (merge `12de2797`: SteamOverlayClient, SteamWishlistService, traduções, libredirect, gradle). Tela preta corrigida (7 bugs: import AHB, finalLayout/transições, atlas→filterOutput, split submissions, cursor loadOp=CLEAR, config não aplicada no launch, crash cold-start). 131 presets libretro embarcados, default `film/technicolor` verificado por pixel-stats. Loop de teste automatizado `tools/shader-test-loop/shader_test_loop.py` (setprop `debug.gamenative.preset` + screencap + logcat; 16/17 shaders OK; `misc/glass` falha compile). Deferred preset reload na render thread (sem SIGSEGV). Commits-chave: `06aef179` (fix), `cf8085aa` (shaders+loop), `fc95cb4b` (docs), `12de2797` (pull upstream). |
| `milestone-2026-08-08-armsx2-inspired-librashader-hardening` | `bff50170` | 2026-08-08 | Shaders librashader endurecidos inspirados no ARMSX2 (emulador de PS2 p/ Android): port dos 3 padrões superiores — params aplicados só na render thread (geração atômica), falha de chain -> fallback pro frame sem shader + latch, e create-first swap (chain nova antes de liberar a antiga; create falho mantém o shader anterior). Sistema de feedback cibernético: renderLoop loga exceções (era catch mudo), waits com timeout 500ms (anti-freeze WaitForFences), logs de path por frame, hook debug.gamenative.preset valida arquivo, loop de teste cena-independente (delta-vs-baseline). Verificado no Mi 11: invert STRONG_CHANGE (+250), technicolor CHANGED (+37), 17/17 sem erros de pipeline. Commits: 9c622426 (feat), bff50170 (docs). Referência: docs/ARMSX2-librashader-vulkan.md |
| `milestone-2026-08-09-quickmenu-gamepad-100pct` | `92f0c4b4` | 2026-08-09 | QuickMenu 100% navegável por joystick no device (bus-level input, padrão LibraryScreen). Causa raiz encontrada e corrigida: menu nascia sem foco (aba sem itens focáveis, ex. TOOLS sem processos) + analógico via event bus (PluviaApp.events) em vez de listener de view (que a view GL da janela do jogo tornava não confiável). Framework compartilhado (GamepadModifiers.kt + GamepadFocus.kt: gamepadSelectable/gamepadAdjustableRow/gamepadBackHandler/GamepadFocusScope + linguagem visual Focada/Selecionada/Travada), 19/19 testes JVM verdes. QuickMenu migrado + L1/R1 abas + L2/R2 página + GamepadActionBar + TOOLS persiste aba + remember-selection. Diálogos (ElementEditor/TouchGesture/ShooterMode/PhysicalController/Binding/pickers) com GamepadFocusScope + B fecha + sliders com A-lock. Strings EN+pt-rBR. Verificado no Mi 11 via harness setprop debug.gamenative.input pelo pipeline real: menu abre com foco (fallback rail), stick 1 linha/gesto, A aplica bead.slangp, A-lock 60->70 FPS, B hierárquico, L1/R1 cíclico, L2/R2 página. Bugs pegos na verificação: crash plurals (pluralStringResource), crash coerceIn lista vazia, orderedTabs congelado. Física (T5-T7 + GamepadActionBar) pendente do usuário. Commits: 92f0c4b4 |
| `milestone-2026-08-10-quickmenu-joystick-full-compat` | `32b6a0e7` | 2026-08-10 | QuickMenu com joystick 100% compatível no device (spec 2026-08-10, commit 32b6a0e7). Causas-raiz do menu morto corrigidas: (1) Compose 1.8.0-beta01 descarta requestFocus silenciosamente quando há alvo de foco ativo obsoleto — 1º open funcionava, reopens morriam; fix clearFocus(true) + verificação de aterrissagem + retry + fallback rail, validado no Mi 11 (PS4 real): reopens aterrissam e navegam. (2) Histerese do stick: GamepadStickLogic puro (re-arm abaixo do dead zone 0.45) compartilhado pelos 2 navegadores + 7 testes JVM (regressão drift 0.40). (3) Guardião de foco contínuo (loop 400ms, não one-shot) cobre toggle No filter/colapso/busca/cross-input — verificado: A em No filter restaura foco e navegação segue. Gaps: PS fecha o menu (ModeKeyBehavior.CloseOverlay, toggle verificado), modo edição navegável (toolbar bootstrap + B=cancelar), linhas de ajuste touch-only (1 nó/linha), toggle desabilitado sem foco-trap, busca rastreada (hasFocus) + IME suprimido quando foco vem do stick, gear/reset com linguagem de foco, INVITE vazio (EN+pt-rBR), painel standalone morto removido. 75/75 testes JVM verdes. Commits: 32b6a0e7 |
| `milestone-2026-08-11-quickmenu-unsolicited-open-fix` | `26465946` | 2026-08-11 | QuickMenu não abre sozinho no start do jogo — causa raiz real encontrada (harness stale) e corrigida + joystick verificado no device (Mi 11). Fix do harness (82496415): debug.gamenative.input stale de sessão anterior disparava BACK no primeiro poll → menu abria ~8s após abrir o jogo; baseline agora é o valor na composição. Invite regression (bac07811, spec 2026-08-11): grace 20s + dedupe 60s + flag por timestamp (pausa correta) para sessões launchBionicSteam=true (host não limpa POLL reabriria o menu para sempre) — válido mas NÃO era o gatilho do sintoma (todos os containers têm o flag false). Navegação joystick T1–T6 verificada no device via harness: stick 1 linha/gesto, busca com IME suprimido, L1/R1 abas, L2/R2 página, A/B hierárquico, reopens sem menu morto; 26/26 testes JVM verdes. T7/T8 (modo edição, diálogos) pendentes de controle físico. Commits: 82496415, 26465946, 77057a5c, 5c5aac5e, 1a88f433 |
| `milestone-2026-08-11-slang-shaders-on-demand` | `f8b39e45` | 2026-08-11 | **Shaders libretro sob demanda (spec 2026-08-11-slang-shaders-on-demand.md)** — APK não embarca mais 131 presets (~1,5 MB, 301 arquivos); carrega só `catalog.json` (604 KB): catálogo completo de 2.541 presets/35 famílias navegável offline. Pack único de ~53 MB (closure de dependências, 1.915 arquivos, commit pinado `a7f04a06`) baixado sob demanda com: URL por commit (não branch), pré-checks de espaço (`packBytes×2+16MB`, `StatFs`) e rede medida (diálogo com tamanho), cancelamento (OkHttp Call retido + tmp limpo), extração whitelist + anti-path-traversal, rename atômico + marker `.complete` (integridade §4.3 → status INSTALLED/UPDATE_AVAILABLE). UI: browser full-screen (busca primeiro, paginação 12, famílias curadas, `bezel` no fim), ícones nuvem/quebrado, CTA com tamanho real, linha de estado do pack com remoção confirmada (não desliga o shader), auto-aplicar preset pedido após install (1 decisão, não 2.541). Migração §6: re-resolução de `relativePath` (casos 1–4, função pura + persistência do novo absoluto) + limpeza única de `filesDir/retroarch*` no boot. Navegação persistente (pedido do usuário): estado do browser hoisted em `ShaderSectionState` — reabrir volta ao nível do shader escolhido; instalação sobrevive ao fechar o browser; re-pick de preset migrado carrega (fix toggle-off só quando carregado). Testes: 22/22 shader+tar+resolve verdes + 88 da trinca gamepad/IME/foco; `assembleModernDebug` OK. Verificação on-device (§8.2) **pendente — sem dispositivo** (dispositivo travou com PIN após reboot; usuário sem device disponível). |
| `2026-08-12-shader-per-preset-download` | `017dfe7c` | 2026-08-12 | **Download por-preset (decisão do usuário)** — substitui o pack único de ~53 MB: nada é baixado por padrão; escolher um shader baixa **só a closure dele** (`deps` no catálogo: `.slangp` + passes + `#include` + LUTs + `#reference`; ex. `crt/crt-easymode` = 6,4 KB, Mega_Bezel ≈ 5,6 MB) de `raw.githubusercontent.com` no commit pinado, com reuso de arquivos já em cache. `sync_slang_shaders.py` agora persiste `deps` por preset (e inclui `.slangp` referenciados na closure). UI: sem CTA de pack — linha do preset mostra progresso/falha/retry, re-pick durante download cancela, sucesso aplica automaticamente. Pré-checks de espaço/metered mantidos por closure. Validação sem device: smoke test na rede — 1.786 arquivos amostrados + 2.541 presets resolvem no commit `a7f04a06`; 95/95 testes JVM verdes; `assembleModernDebug` OK. On-device §8.2 segue pendente (sem dispositivo). |
| `milestone-2026-08-12-shader-browser-focus-double-click` | `0a50f643` | 2026-08-12 | **Browser de shaders: foco à prova de ida-e-volta + duplo-clique "aplica e fecha" (spec 2026-08-12)**. Missão 1: guardião de foco gateado por `!shaderBrowserOpen` (não trabalha com o browser aberto — sem ruído/corrida contra o bootstrap do browser). Missão 2: fechar o browser restaura foco imediatamente via `requestMenuFocus()` (latch `browserWasOpen` evita duplo walk-down na abertura). Missão 3: reset defensivo de `shaderBrowserOpen` quando o menu fecha por qualquer caminho (reabrir cai na aba EFFECTS, nunca no browser). Missão 4 (sem código): verificação do BackHandler empilhado documentada. Missão 5: duplo-clique — `ShaderDoubleClickLogic` puro (janela 300 ms, arma só quando `applyPreset` retornou true, linha em nuvem/falha nunca arma; 2º clique na mesma linha dentro da janela = aplica-e-fecha; fora da janela o toggle-off do preset ativo permanece; toque e gamepad pelo mesmo `onClick`), integrado no `PresetRow` + `onCloseQuickMenu = onDismiss`. 101/101 testes JVM verdes (`ShaderDoubleClickLogicTest` 6 casos) + `assembleModernDebug` OK. Verificação on-device F1–F10 **pendente — sem dispositivo**. |
| `2026-08-12-quickmenu-gamepad-ux-review` | `682662a8` | 2026-08-12 | **Revisão de UX de gamepad do QuickMenu — P1–P6 (spec 2026-08-12-quickmenu-gamepad-ux-review.md, padrões PS4/Xbox)**. P1: START/SELECT consumidos com o overlay aberto (bridge) + START vira segundo toggle do QuickMenu espelhando HOME (abre com o menu fechado no XServerScreen; fecha com o menu aberto na bridge; resume manual preservado). P2: L1/R1 com gate de repeatCount (segurar não cicla abas); L2/R2 continuam repetindo. P3: GamepadActionBar no browser de shaders (A=Selecionar, B=Voltar, PS=Fechar; novo GamepadButton.GUIDE + strings EN/pt). P4: L1/R1 troca de aba foca o CONTEÚDO (walk-down G9), não o rail — 1 pressão a menos; fallback para o rail em abas sem itens. P5: L2/R2 paginam movendo o FOCO (N passos = meia viewport), seleção acompanha a rolagem; scroll puro só a partir do rail. P6: botão fechar (X) alcançável — rota explícita focusProperties.up no rail/conteúdo + focusRequester próprio. Harness: gamepadDeviceId() pontua devices (DS4 expõe controle+touchpad+sensores; touchpad anuncia GAMEPAD e era escolhido). Verificado on-device (Mi 11 + DS4): P1 abrir/fechar/consumir ✓, P4 ✓ (QMFocus row 0 após troca), P5 ✓ (R2⇒row 1→2, L2⇒row 0), P6 ✓ (Up+X+A fecha o menu), P3 ✓ (footer renderizado). 101/101 testes JVM verdes. Duplo-clique A-A e F1–F10: confirmação manual com o usuário em andamento. |
| `2026-08-12-shader-closure-fix` | `5c6bf96c` | 2026-08-12 | **Shaders voltam a aplicar — closure-aware resolve + parser endurecido (spec 2026-08-12-shader-closure-fix.md)**. Causa raiz: `film/technicolor.slangp` tem linha de texturas malformada no upstream (`textures = "SamplerLUT;noise1;` — aspa não terminada); o regex do sync caía no fallback capturando o token COM a aspa líder (`"SamplerLUT`), que nunca casava com a key — a LUT `cmyk-16.png` ficava fora dos deps e o chain create do librashader falhava SILENCIOSAMENTE ("baixa mas não aplica"). Mesma classe em +4 presets (hyllian CRT LUTs, downsample multiLUT, pal nes_lut, grade-rgb). Fix 1: `TEXTURES_LINE` dedicada no sync (captura o resto da linha cru, tolerante a aspas/espaços/comentários, acumulação multi-linha, warning p/ token com whitespace) + catalog regenerado (determinístico, 7 broken inalterados). Fix 2: `resolveShaderConfig` closure-aware via `closureComplete` — closure incompleta resolve para "seleção visível, nada carregado"; re-pick baixa SÓ os faltantes; call sites menu + boot (persiste path limpo). Fix 3 (self-heal UX): `ShaderToggleSubtitle` puro — EFFECTS tab mostra "Não baixado — re-selecione no navegador" em vez do nome do preset. Testes: 41/41 shader+browser verdes (`ShaderToggleSubtitleTest` 4 + `ShaderConfigResolveTest` +4). On-device pendente: self-heal do technicolor pré-baixado. |
| `2026-08-12-quickmenu-gamepad-pipeline-hardening` | `55337822` | 2026-08-12 | **Pipeline de gamepad do QuickMenu endurecido — fim das contradições de roteamento (spec 2026-08-12-quickmenu-gamepad-pipeline-hardening.md)**. C1: roteamento vivo — novo holder `OverlayInputState` escrito na composição e lido pelos handlers no call time (o `val` derivado capturado em `DisposableEffect(Unit)` era stale para sempre: jogo recebia input atrás do menu). C2: single-writer — com overlay aberto o branch de jogo (noteGamepadButton/refreshControllerMappingsForHotplug/winHandler) é inalcançável; 0 hotplug em todos os ciclos on-device. C3: registry por identidade (`===`) + `listenerCount()` + `rememberUpdatedState(onCloseOverlay)` no bridge + `dismissOverlayMenu` estável — 20+ ciclos abrir/fechar com on==off e 0 `matched NOTHING`. C4: dedupe DPAD×hat (`GamepadMoveDedupe`, janela 120 ms, 8 testes JVM) — 1 gesto = 1 movimento. C5: bootstrap de foco serializado por mutex único + guardião gentil (skip < 600 ms) — walk-down duplo impossível, "user navigating, skipping cycle" observado. C6: guardião próprio do browser (`ShaderBrowser guardian: restoring focus row=16` em 133 ms após "Show more" remover a linha focada). D1: auditoria de dead code — `FocusRing.kt` MANTIDO (13+ callers na biblioteca; premissa "zero callers" do spec incorreta), `ModeKeyBehavior.None` explícito no edit mode. M8: instrumentação `GamepadTrace`/`GamepadRoute`/`EventBus`/listening-stopped. Descoberta: PS real = `KEYCODE_BUTTON_MODE` 110 (não 188); harness + verify script corrigidos. Verificado on-device (Mi 11 + DS4, Silksong): V2/V1/V7/V6/M6 PASS. |
| `milestone-2026-08-12-projeto-completo-libshader-melonds-armsx2` | `33e96e4a` | 2026-08-12 | PROJETO COMPLETO — implementação do libshader (librashader) inspirada nos emuladores melonDS e ARMSX2. Fim da jornada de shaders RetroArch do fork GameNative: (1) melonDS / melonDS-android (Rafael Caetano, v0.7.0.rc2) contribuiu a topologia offscreen → sampler → swapchain present, o tracking de layout de imagem e as barreiras de memória largas que mantêm o driver Adreno honesto (commits 06aef179, 54a271ca); (2) ARMSX2 (fork ARM64 do PCSX2) contribuiu a aplicação de parâmetros apenas na render thread, o fallback de chain com latch (preset quebrado degrada para o frame sem shader, nunca tela preta) e o create-first swap (o preset novo só troca quando o chain novo compila). Entrega final: 131 presets libretro embarcados, troca ao vivo sem restart, download por demanda, closure-aware resolve (shaders voltam a aplicar, spec 2026-08-12-shader-closure-fix), browser de shaders com foco/duplo-clique e pipeline de gamepad do QuickMenu endurecido (spec 2026-08-12-quickmenu-gamepad-pipeline-hardening: roteamento vivo, registry por identidade, dedupe DPAD×hat, bootstrap serializado, guardiões). Verificado on-device (Xiaomi Mi 11 / Adreno 650, Silksong, DS4): shaders aplicando + 91 testes JVM verdes. |
| `milestone-2026-08-13-shader-browser-ime-explicit` | `72e1c206` | 2026-08-13 | Teclado do browser de shaders nunca abre sozinho (spec 2026-08-13-shader-browser-ime-bootstrap) — causa raiz: campo de busca é a linha 0 do Home e o bootstrap/guardião de foco o foca programaticamente; o Compose abre o IME em qualquer ganho de foco, e o gate arrivedViaGamepad (relógio de movimento < 400ms) falhava ao chegar no Explorar via X ou com move antigo. Regra nova: supressão incondicional quando o foco chega sem intenção explícita (X ou toque); toque registra intenção no primeiro down (antes do foco aterrar) com reset no fim do gesto se o foco não aterrou (anti-vazamento de padding tap). arrivedViaGamepad removido (lógica morta) + 4 testes. 55 tasks verdes. Verificação on-device I1-I8 pendente (sem dispositivo). |
| `milestone-2026-08-14-onda2-pos-implementacao` *(tag pendente — verificação on-device)* | `cb3cd03a` | 2026-08-14 | Pós-implementação da Onda 2 do gamepad universal (spec 2026-08-14-gamepad-onda2-pos-implementacao, M1–M4): (M1/L1+L6) `GamepadProfileStore` com cache em memória por instância + `GamepadHub` com cache de perfil efetivo por (deviceId, appId) invalidado em hotplug/save (`invalidateProfiles`), `defaultDeadzones` cacheado — `onAxis` sem `file.readText` e sem alocação do default por MotionEvent; (M2/L3) seção de settings "Gamepad" com o gate `gamepadUniversalEnabled`, swap OK/Cancel (equiv. `menu_swap_ok_cancel_buttons` do RetroArch) e deadzones globais do stick (jogo/menu) — linhas `gamepadSelectable` + sliders com A-lock, strings EN/pt-rBR; (M3/L2) remap alcançável pela UI (botão "Remapear controles" na seção, desabilitado sem device) com persistência via `hub.saveDeviceProfile` (deviceStore keyed por mappingKey) + `invalidateProfiles`; (M4/L4+L5+L7) `deadZone` removido dos navigators (hub é a fonte única), `directionFromResult`/`bindingForKey` removidos do `GamepadViewBridge`, KDoc do X/Y consumido por invariante no `BusGamepadKeyBridge`. 173 testes JVM filtrados verdes; `assembleModernDebug` OK. Verificação on-device pendente (sem dispositivo): M1/O11+ overhead < 1 ms com cache quente, M2 gate liga/desliga pela UI + sliders persistem + swap OK/Cancel no QuickMenu, M3 remap persiste/reabre/conflito, regressão O1–O11 + flip do gate default=true (impl spec). |
| `milestone-2026-08-14-gamepad-intuito-upgrades` *(tag pendente — verificação on-device)* | `2a9ac95e` | 2026-08-14 | Upgrades do doc de intuito (spec 2026-08-14-gamepad-intuito-validacao-upgrades, U1–U7 + V1/V12): **U6** LibraryScreen com OK/Cancel por FaceStyle + swap, atalhos lógicos (LibraryGamepadKeys) e deadzones do menu no bootstrap; **U2** touchpad → mouse (TouchpadProcessor puro, forwarder no ponto do gate — V7, harness `touch:`); **U1** gyro → mouse/câmera (GyroProcessor puro com recenter/histerese, GamepadSensorSource API 31+ com lifecycle V3, ativação por botão, seção Gyro no remap, harness `gyro:`); **V1** store preserva chaves desconhecidas no save (política r2); **U3+U4** camadas completas (LayerResolver HOLD/TOGGLE/DOUBLE_TAP, merge granular, UI de camadas no remap) + remap aplicado ao JOGO (PhysicalControllerHandler consulta a camada universal antes do inject — byte-identical com gate OFF); **U5** rumble por device (API 16/31, efeitos por perfil; ponte Wine/XInput dimensionada); **U7** bateria/capacidades por device (coleta no hotplug, bloco na seção Gamepad). 186 testes JVM filtrados verdes; assembleModernDebug OK. Verificação on-device pendente (bateria Onda 2 + cenários §[H] do quickmenu-verify.sh). |
| *(próximo)* | | | |
