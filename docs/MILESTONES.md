# Milestones do projeto

Mapa dos pontos importantes da história do projeto. Cada milestone é uma **tag anotada**
do git — permanente, nomeada e à prova de GC/reflog. Para voltar a um milestone:

```bash
git switch -c <nome-do-branch> milestone-<data>-<nome>   # ex.:
git switch -c retorno-milestone milestone-2026-08-07-librashader-funcional
```

Novos milestones: `tools/milestone.sh <nome-curto> "<descrição>"` (cria tag + registra aqui + push).

---

| Milestone (tag) | Commit (HEAD na época) | Data | O que marca |
|---|---|---|---|
| `milestone-2026-08-07-librashader-funcional` | `12de2797` | 2026-08-07 | **Shaders RetroArch (librashader) FUNCIONANDO no device** (Xiaomi Mi 11/Adreno 650) **+ pull do upstream utkarshdalal:master incluído** (merge `12de2797`: SteamOverlayClient, SteamWishlistService, traduções, libredirect, gradle). Tela preta corrigida (7 bugs: import AHB, finalLayout/transições, atlas→filterOutput, split submissions, cursor loadOp=CLEAR, config não aplicada no launch, crash cold-start). 131 presets libretro embarcados, default `film/technicolor` verificado por pixel-stats. Loop de teste automatizado `tools/shader-test-loop/shader_test_loop.py` (setprop `debug.gamenative.preset` + screencap + logcat; 16/17 shaders OK; `misc/glass` falha compile). Deferred preset reload na render thread (sem SIGSEGV). Commits-chave: `06aef179` (fix), `cf8085aa` (shaders+loop), `fc95cb4b` (docs), `12de2797` (pull upstream). |
| `milestone-2026-08-08-armsx2-inspired-librashader-hardening` | `bff50170` | 2026-08-08 | Shaders librashader endurecidos inspirados no ARMSX2 (emulador de PS2 p/ Android): port dos 3 padrões superiores — params aplicados só na render thread (geração atômica), falha de chain -> fallback pro frame sem shader + latch, e create-first swap (chain nova antes de liberar a antiga; create falho mantém o shader anterior). Sistema de feedback cibernético: renderLoop loga exceções (era catch mudo), waits com timeout 500ms (anti-freeze WaitForFences), logs de path por frame, hook debug.gamenative.preset valida arquivo, loop de teste cena-independente (delta-vs-baseline). Verificado no Mi 11: invert STRONG_CHANGE (+250), technicolor CHANGED (+37), 17/17 sem erros de pipeline. Commits: 9c622426 (feat), bff50170 (docs). Referência: docs/ARMSX2-librashader-vulkan.md |
| `milestone-2026-08-09-quickmenu-gamepad-100pct` | `92f0c4b4` | 2026-08-09 | QuickMenu 100% navegável por joystick no device (bus-level input, padrão LibraryScreen). Causa raiz encontrada e corrigida: menu nascia sem foco (aba sem itens focáveis, ex. TOOLS sem processos) + analógico via event bus (PluviaApp.events) em vez de listener de view (que a view GL da janela do jogo tornava não confiável). Framework compartilhado (GamepadModifiers.kt + GamepadFocus.kt: gamepadSelectable/gamepadAdjustableRow/gamepadBackHandler/GamepadFocusScope + linguagem visual Focada/Selecionada/Travada), 19/19 testes JVM verdes. QuickMenu migrado + L1/R1 abas + L2/R2 página + GamepadActionBar + TOOLS persiste aba + remember-selection. Diálogos (ElementEditor/TouchGesture/ShooterMode/PhysicalController/Binding/pickers) com GamepadFocusScope + B fecha + sliders com A-lock. Strings EN+pt-rBR. Verificado no Mi 11 via harness setprop debug.gamenative.input pelo pipeline real: menu abre com foco (fallback rail), stick 1 linha/gesto, A aplica bead.slangp, A-lock 60->70 FPS, B hierárquico, L1/R1 cíclico, L2/R2 página. Bugs pegos na verificação: crash plurals (pluralStringResource), crash coerceIn lista vazia, orderedTabs congelado. Física (T5-T7 + GamepadActionBar) pendente do usuário. Commits: 92f0c4b4 |
| `milestone-2026-08-10-quickmenu-joystick-full-compat` | `32b6a0e7` | 2026-08-10 | QuickMenu com joystick 100% compatível no device (spec 2026-08-10, commit 32b6a0e7). Causas-raiz do menu morto corrigidas: (1) Compose 1.8.0-beta01 descarta requestFocus silenciosamente quando há alvo de foco ativo obsoleto — 1º open funcionava, reopens morriam; fix clearFocus(true) + verificação de aterrissagem + retry + fallback rail, validado no Mi 11 (PS4 real): reopens aterrissam e navegam. (2) Histerese do stick: GamepadStickLogic puro (re-arm abaixo do dead zone 0.45) compartilhado pelos 2 navegadores + 7 testes JVM (regressão drift 0.40). (3) Guardião de foco contínuo (loop 400ms, não one-shot) cobre toggle No filter/colapso/busca/cross-input — verificado: A em No filter restaura foco e navegação segue. Gaps: PS fecha o menu (ModeKeyBehavior.CloseOverlay, toggle verificado), modo edição navegável (toolbar bootstrap + B=cancelar), linhas de ajuste touch-only (1 nó/linha), toggle desabilitado sem foco-trap, busca rastreada (hasFocus) + IME suprimido quando foco vem do stick, gear/reset com linguagem de foco, INVITE vazio (EN+pt-rBR), painel standalone morto removido. 75/75 testes JVM verdes. Commits: 32b6a0e7 |
| `milestone-2026-08-11-quickmenu-unsolicited-open-fix` | `26465946` | 2026-08-11 | QuickMenu não abre sozinho no start do jogo — causa raiz real encontrada (harness stale) e corrigida + joystick verificado no device (Mi 11). Fix do harness (82496415): debug.gamenative.input stale de sessão anterior disparava BACK no primeiro poll → menu abria ~8s após abrir o jogo; baseline agora é o valor na composição. Invite regression (bac07811, spec 2026-08-11): grace 20s + dedupe 60s + flag por timestamp (pausa correta) para sessões launchBionicSteam=true (host não limpa POLL reabriria o menu para sempre) — válido mas NÃO era o gatilho do sintoma (todos os containers têm o flag false). Navegação joystick T1–T6 verificada no device via harness: stick 1 linha/gesto, busca com IME suprimido, L1/R1 abas, L2/R2 página, A/B hierárquico, reopens sem menu morto; 26/26 testes JVM verdes. T7/T8 (modo edição, diálogos) pendentes de controle físico. Commits: 82496415, 26465946, 77057a5c, 5c5aac5e, 1a88f433 |
| `milestone-2026-08-11-slang-shaders-on-demand` | `f8b39e45` | 2026-08-11 | **Shaders libretro sob demanda (spec 2026-08-11-slang-shaders-on-demand.md)** — APK não embarca mais 131 presets (~1,5 MB, 301 arquivos); carrega só `catalog.json` (604 KB): catálogo completo de 2.541 presets/35 famílias navegável offline. Pack único de ~53 MB (closure de dependências, 1.915 arquivos, commit pinado `a7f04a06`) baixado sob demanda com: URL por commit (não branch), pré-checks de espaço (`packBytes×2+16MB`, `StatFs`) e rede medida (diálogo com tamanho), cancelamento (OkHttp Call retido + tmp limpo), extração whitelist + anti-path-traversal, rename atômico + marker `.complete` (integridade §4.3 → status INSTALLED/UPDATE_AVAILABLE). UI: browser full-screen (busca primeiro, paginação 12, famílias curadas, `bezel` no fim), ícones nuvem/quebrado, CTA com tamanho real, linha de estado do pack com remoção confirmada (não desliga o shader), auto-aplicar preset pedido após install (1 decisão, não 2.541). Migração §6: re-resolução de `relativePath` (casos 1–4, função pura + persistência do novo absoluto) + limpeza única de `filesDir/retroarch*` no boot. Navegação persistente (pedido do usuário): estado do browser hoisted em `ShaderSectionState` — reabrir volta ao nível do shader escolhido; instalação sobrevive ao fechar o browser; re-pick de preset migrado carrega (fix toggle-off só quando carregado). Testes: 22/22 shader+tar+resolve verdes + 88 da trinca gamepad/IME/foco; `assembleModernDebug` OK. Verificação on-device (§8.2) **pendente — sem dispositivo** (dispositivo travou com PIN após reboot; usuário sem device disponível). |
| *(próximo)* | | | |
